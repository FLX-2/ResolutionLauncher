<Window x:Class="ResolutionLauncherModern.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resolution Launcher"
        Width="340" Height="320"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Style="{StaticResource MicaWindowStyle}"
        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">

    <Border x:Name="MainBorder" CornerRadius="8" Background="#FF2D2D2D" BorderBrush="#1AFFFFFF" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="#1AFFFFFF" CornerRadius="8,8,0,0">
                <Grid>
                    <TextBlock Text="Resolution Launcher" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="16,0" FontSize="12" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource WindowControlButton}" Content="&#xE921;" Click="Minimize_Click"/>
                        <Button Style="{StaticResource WindowControlButton}" Content="&#xE8BB;" Click="Close_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="16">
                <!-- Mode Toggle -->
                <Border Background="#1AFFFFFF" CornerRadius="4" Padding="2">
                    <UniformGrid Columns="2">
                        <RadioButton x:Name="ExeMode" Content="EXE/LNK" Style="{StaticResource ModeToggleStyle}" IsChecked="True" Checked="ModeToggle_Checked"/>
                        <RadioButton x:Name="UwpMode" Content="UWP" Style="{StaticResource ModeToggleStyle}" Checked="ModeToggle_Checked"/>
                    </UniformGrid>
                </Border>

                <!-- Target Input -->
                <StackPanel x:Name="ExeTarget" Margin="0,12,0,0">
                    <TextBlock Text="Target" FontSize="12" FontWeight="Normal" Foreground="#CCCCCC" Margin="0,0,0,4"/>
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TargetPath" IsReadOnly="True" Grid.Column="0" Margin="0,0,8,0" Text="Select application..."/>
                        <Button Content="Browse..." Grid.Column="1" Style="{StaticResource Windows11ButtonSecondary}" MinWidth="80" Click="BrowseButton_Click"/>
                    </Grid>
                </StackPanel>

                <StackPanel x:Name="UwpTarget" Margin="0,12,0,0" Visibility="Collapsed">
                    <TextBlock Text="UWP Package" FontSize="12" FontWeight="Normal" Foreground="#CCCCCC" Margin="0,0,0,4"/>
                    <TextBox x:Name="UwpPath" Margin="0,8,0,0" Text="microsoft.minecraftuwp://"/>
                </StackPanel>

                <!-- Resolution -->
                <StackPanel Margin="0,12,0,0">
                    <TextBlock Text="Resolution" FontSize="12" FontWeight="Normal" Foreground="#CCCCCC" Margin="0,0,0,4"/>
                    <ComboBox x:Name="ResolutionCombo" SelectedIndex="0" Height="32" Margin="0,8,0,0">
                        <ComboBoxItem Content="1920×1080"/>
                        <ComboBoxItem Content="2560×1440"/>
                        <ComboBoxItem Content="3840×2160"/>
                        <ComboBoxItem Content="1600×900"/>
                        <ComboBoxItem Content="1280×720"/>
                    </ComboBox>
                </StackPanel>

                <!-- Action Buttons -->
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Shortcut" Style="{StaticResource Windows11ButtonSecondary}" Margin="0,0,4,0" Height="36"/>
                    <Button Grid.Column="1" Content="Launch" Style="{StaticResource Windows11ButtonPrimary}" Margin="4,0,0,0" Height="36" Click="LaunchButton_Click"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</Window>